package com.aylien.secretsanta.participant;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeParticipantResourceIT extends ParticipantResourceTest {

    // Execute the same tests but in native mode.
}